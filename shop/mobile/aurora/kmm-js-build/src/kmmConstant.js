import shared from "kmm-module/shared";

// get constant from kmm (it's work)
function getConstantEmail() {
    return `${shared.com.keygenqt.shop.utils.constants.AppConstants.other.CONTACT_EMAIL}`
}

export default getConstantEmail;